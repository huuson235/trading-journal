# Root admin (quản lý accounts) — không dùng journal riêng
AUTH_USERNAME=admin
AUTH_PASSWORD=admin

# Khi migrate journal.db cũ → account đầu tiên (chỉ chạy 1 lần)
# MIGRATE_ACCOUNT_USERNAME=main
# MIGRATE_ACCOUNT_PASSWORD=main